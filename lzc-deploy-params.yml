params:
    - id: admin_password
      type: string
      name: "admin auth password"
      description: "auth key for admin login"

locales:
    zh:
        admin_password:
            name: "管理员密码"
            description: "管理员密码，用于保护管理UI界面"
