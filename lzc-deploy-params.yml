params:
    - id: admin_password
      type: secret
      name: "Admin Password"
      description: "MASTERPASS used to unlock the Drizzle Gateway web UI"
      default_value: "$random(len=20)"
      optional: false

locales:
    zh:
        admin_password:
            name: "管理员密码"
            description: "用于保护 Drizzle Gateway 管理界面的 MASTERPASS"
